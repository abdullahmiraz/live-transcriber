# Optional overrides. By default the client uses relative paths (/api, /ws) which work
# both behind nginx and via the Vite dev proxy, so these are usually not needed.
PUBLIC_API_BASE=/api
PUBLIC_WS_PATH=/ws
