root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  include_ext = ["go", "sql"]
  exclude_dir = ["tmp", "vendor"]
  delay = 400
  stop_on_error = false

[log]
  time = true

[color]
  main = "cyan"
  build = "yellow"
  runner = "green"
